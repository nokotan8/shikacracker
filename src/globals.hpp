#include <string>

// Variables set via command-line args.
// They are not changed anywhere outside of main.
extern int quiet_flag;
extern int num_threads;
extern int hash_mode;
extern int atk_mode;
extern char separator;
extern std::string dict;
extern std::string mask;
